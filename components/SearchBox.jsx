import React from "react";
import { View } from "react-native";
import { Input } from "react-native-elements";

const SearchBox = props => {
  return (
    <View>
      <Input placeholder="Search what you need" />
    </View>
  );
};

export default SearchBox;
