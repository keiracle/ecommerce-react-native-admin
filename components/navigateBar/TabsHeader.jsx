import React from "react";
import styled from "styled-components/native";

const TabsHeader = (props) => {
  return <STabsHeader></STabsHeader>;
};

const STabsHeader = styled.View``;

export default TabsHeader;
